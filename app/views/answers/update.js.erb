$('.answer-errors').html('<%= render 'shared/errors', resource: @answer %>');

<% if !@answer.errors.present? %>
  $('.answers').html('<%= j render @question.answers %>');
  $('.answer-file').html('<%= j render 'shared/attachments', resource: @answer %>')
<% end %>
